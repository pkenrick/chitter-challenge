<!DOCTYPE html>
<html>
  <head>
    <title>Bookmark Manager</title>
  </head>

  <body>
    <% if flash[:message] %>
      <%= flash[:message] %>
    <% end %>
    <% if session[:current_user] %>
    <p>Welcome, <%= session[:current_user].name %>!</p>
    <form action='/users/sign_out' method='post'>
      <input type='submit' value='Sign out'>
    </form>
  <% end %>
  <%= yield %>
    
  
  
  </body>
</html>
